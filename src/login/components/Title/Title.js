import React from 'react';
import './Title.css';

const Title = () => {
    return (
        <div className="title-container">
            <label> LOGIN </label>
        </div>
    )
};

export default Title;
